import React,{useEffect,useState} from 'react'; import {api} from '../services/api';
export default function Problems({onStart}){const [items,setItems]=useState([]); useEffect(()=>{api.problems().then(setItems)},[]); return <section><div className="hero"><p className="eyebrow">PRACTICE LOOP</p><h1>Design. Submit. Learn. Try again.</h1><p>Practice object-oriented design problems and get explainable feedback on responsibilities, abstractions, relationships, and extensibility.</p></div><div className="grid">{items.map(p=><article className="card" key={p.id}><div className="row"><span className="pill">{p.difficulty}</span></div><h2>{p.title}</h2><p>{p.description}</p><button className="primary" onClick={()=>onStart(p)}>Start practice →</button></article>)}</div></section>}
