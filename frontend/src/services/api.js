const BASE='http://localhost:4000/api';
async function request(path, options={}) { const r=await fetch(BASE+path,{headers:{'Content-Type':'application/json',...(options.headers||{})},...options}); const data=await r.json(); if(!r.ok) throw new Error(data.error||data.message||'Request failed'); return data; }
export const api={problems:()=>request('/problems'), problem:id=>request(`/problems/${id}`), createAttempt:problemId=>request('/attempts',{method:'POST',body:JSON.stringify({problemId})}), updateAttempt:(id,submission)=>request(`/attempts/${id}`,{method:'PUT',body:JSON.stringify({submission})}), submit:(id,submission)=>request(`/attempts/${id}/submit`,{method:'POST',body:JSON.stringify({submission})}), attempts:()=>request('/attempts'), attempt:id=>request(`/attempts/${id}`)};
