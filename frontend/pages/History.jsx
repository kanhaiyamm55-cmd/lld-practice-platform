import React,{useEffect,useState} from 'react'; import {api} from '../services/api';
export default function History({onReview}){const [items,setItems]=useState([]); useEffect(()=>{api.attempts().then(setItems)},[]); return <section><p className="eyebrow">PROGRESS</p><h1>Practice history</h1><p>Review previous attempts and see whether your design improves over time.</p><div className="history">{items.length===0?<div className="empty">No attempts yet. Start with Parking Lot.</div>:items.map(a=><div className="history-row" key={a.id}><div><strong>{a.problemId.replaceAll('-',' ')}</strong><small>{new Date(a.createdAt).toLocaleString()}</small></div><span>{a.status==='COMPLETED'?`${a.score}/100`:a.status}</span><button onClick={()=>onReview(a)}>Review</button></div>)}</div></section>}
