import React from 'react'; import ScoreBar from '../components/ScoreBar';
export default function Feedback({attempt,onBack,onRetry}){const r=attempt.feedback; return <section><div className="feedback-head"><div><p className="eyebrow">EVALUATION COMPLETE</p><h1>Your design review</h1><p>{r?.summary}</p></div><ScoreBar score={attempt.score||0}/></div><div className="feedback-grid"><div className="card"><h2>Strengths</h2>{(r?.strengths||[]).map((s,i)=><div className="strength" key={i}>✓ {s}</div>)}</div><div className="card"><h2>Actionable feedback</h2>{(r?.feedback||[]).map((f,i)=><div className="feedback-item" key={i}><div className="row"><strong>{f.category}</strong><span>{f.score}/{f.maxScore}</span></div><p className={`severity ${f.severity}`}>{f.severity}</p><p>{f.message}</p><small>Suggestion: {f.suggestion}</small></div>)}</div></div><div className="actions"><button onClick={onBack}>← Problems</button><button className="primary" onClick={onRetry}>Try again</button></div></section>}
