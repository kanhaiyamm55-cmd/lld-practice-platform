import test from 'node:test'; import assert from 'node:assert/strict';
import { RuleBasedEvaluator } from '../src/evaluation/RuleBasedEvaluator.js';
const problem={id:'parking-lot'}; const evaluator=new RuleBasedEvaluator();
test('strong structured submission receives useful feedback',()=>{const r=evaluator.evaluate(problem,{classes:[{name:'ParkingLot'},{name:'Vehicle'},{name:'Ticket'}],interfaces:[{name:'PricingStrategy'}],relationships:[{from:'ParkingLot',to:'Vehicle',type:'association'},{from:'ParkingLot',to:'Ticket',type:'composition'}],explanation:'Single responsibility keeps parking orchestration separate from pricing. Open closed design lets us add pricing strategies without changing ParkingLot.'}); assert.equal(r.feedback.length,6); assert.ok(r.score>=80);});
test('empty submission is not treated as correct',()=>{const r=evaluator.evaluate(problem,{}); assert.ok(r.score<60); assert.ok(r.feedback.some(x=>x.severity==='medium'));});
test('reasoning is rewarded',()=>{const base={classes:[{name:'A'},{name:'B'},{name:'C'}],interfaces:[{name:'Strategy'}],relationships:[{from:'A',to:'B'},{from:'B',to:'C'}]}; const short=evaluator.evaluate(problem,{...base,explanation:'ok'}); const long=evaluator.evaluate(problem,{...base,explanation:'Single responsibility separates orchestration from policies. Strategy isolates changing pricing rules and keeps dependencies pointed at abstractions. This makes adding weekend pricing easier.'}); assert.ok(long.score>short.score);});
